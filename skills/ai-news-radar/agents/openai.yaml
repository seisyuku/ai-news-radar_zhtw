interface:
  display_name: "AI News Radar"
  short_description: "Maintain the AI business intelligence radar"
  default_prompt: "Use $ai-news-radar to inspect the current project authority, then safely diagnose or implement this source, data, UI, Actions, or Pages change."
policy:
  allow_implicit_invocation: true
